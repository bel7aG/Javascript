let canvas = document.createElement('canvas');
let ctx = canvas.getContext('2d');

document.body.appendChild(canvas);

let W = canvas.width =  width.innerWidth;
let H = canvas.height = height.innerHeight;

let pointer = {
   pos: {
      x: 0,
      y: 0
   },
   offset: {
      x: 0,
      y: 0
   },
   active: false,
   drag: false,
   target: false
};

function 















// particules
class Point {
   constructor() {
      this.x {
         x: x,
         y: y
      };
      this.y {
         x: x,
         y: y
      }
   }





}
